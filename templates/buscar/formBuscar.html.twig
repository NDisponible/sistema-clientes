<div class="caja container border col-md-5">
<div class="col-md-5 offset-md-4 mt-4 mb-4">
{{ form_start(form) }}
<div class="form-group row">
    {{ form_label(form.idCliente, 'Id cliente', {
        'label_attr': {'class': 'col-sm-4 col-form-label'}
    }) }}
</div>
<div class="col-sm-8">
    {{ form_widget(form.idCliente, {'attr': {'class': 'form-control', 'placeholder': 'Id cliente'}}) }}
</div>
<div class="form-group row">
    <div class="col-sm-12">
        {#<button class="btn btn-primary mt-4 ">{{ button_label|default('Buscar') }}</button>#}
        <a class="btn btn-primary mt-4" href="{{ path('buscarCliente')}}">Buscar</a>
    </div>
</div>
{{ form_end(form) }}
</div>
</div>